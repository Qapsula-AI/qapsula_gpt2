# Настройка JWT авторизации с Vue.js фронтендом

Данное руководство описывает процесс настройки и тестирования системы авторизации для Qapsula RAG системы.

## Архитектура

Система использует:
- **Backend**: FastAPI с JWT (JSON Web Tokens) для stateless авторизации
- **Frontend**: Vue.js 3 SPA, встроенный в FastAPI (монолитная архитектура)
- **Безопасность**: Bcrypt для хеширования паролей, Bearer токены, OAuth2 flow
- **База данных**: SQLAlchemy для управления пользователями

## Структура проекта

```
app/
├── auth/                          # Модуль авторизации
│   ├── __init__.py               # Экспорт функций
│   ├── password.py               # Bcrypt хеширование
│   ├── jwt_handler.py            # JWT токены
│   ├── schemas.py                # Pydantic модели
│   └── dependencies.py           # FastAPI dependencies
│
├── frontend/                      # Vue.js приложение
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router.js
│       ├── composables/
│       │   └── useAuth.js        # Логика авторизации
│       ├── components/
│       │   └── LoginForm.vue
│       └── views/
│           ├── Login.vue
│           └── Dashboard.vue
│
├── static/                        # Собранный фронтенд (после npm run build)
│   ├── index.html
│   └── assets/
│
└── api/
    └── fastapi_app.py            # FastAPI с auth endpoints и SPA serving
```

## Шаг 1: Установка зависимостей

### Backend зависимости

```bash
# Установите Python зависимости
pip install python-jose[cryptography] passlib[bcrypt] python-multipart
```

Или установите все зависимости из requirements.txt:
```bash
pip install -r requirements.txt
```

### Frontend зависимости

```bash
# Перейдите в директорию фронтенда
cd app/frontend

# Установите npm зависимости
npm install
```

## Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Настройте следующие переменные:

```env
# === JWT Авторизация ===
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
ADMIN_PASSWORD=your-secure-admin-password
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### Генерация SECRET_KEY

**ВАЖНО:** Используйте криптографически стойкий ключ:

```bash
# Linux/Mac/WSL
openssl rand -hex 32

# Windows PowerShell
$bytes = New-Object byte[] 32
[Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
[BitConverter]::ToString($bytes).Replace("-", "").ToLower()
```

Скопируйте сгенерированный ключ в `.env`:
```env
SECRET_KEY=a1b2c3d4e5f6...  # Ваш сгенерированный ключ
```

### Установка пароля администратора

Задайте надежный пароль для admin пользователя:
```env
ADMIN_PASSWORD=My$ecureP@ssw0rd2025
```

## Шаг 3: Инициализация базы данных

### Применить миграции

```bash
# Создать таблицы базы данных
alembic upgrade head
```

### Создать admin пользователя

Запустите скрипт создания администратора:

```bash
# Из корня проекта
python scripts/create_admin.py
```

Вывод должен быть примерно таким:
```
============================================================
Создание администратора системы
============================================================

✅ Admin пользователь создан успешно!
   ID: 1
   Username: admin
   Email: admin@qapsula.local
   Является суперпользователем: Да

🔐 Используйте эти учетные данные для входа:
   Username: admin
   Password: My$ecureP@ssw0rd2025

⚠️  ВАЖНО: Сохраните эти данные в надежном месте!
```

**Из Docker контейнера:**
```bash
docker-compose -f docker-compose.dev.yml exec app python scripts/create_admin.py
```

## Шаг 4: Сборка фронтенда

Соберите Vue.js приложение:

```bash
# Из директории app/frontend
cd app/frontend
npm run build
```

Файлы будут собраны в `app/static/`:
```
app/static/
├── index.html
└── assets/
    ├── index-xxx.js
    └── index-xxx.css
```

**Для разработки с hot reload:**
```bash
# В отдельном терминале
cd app/frontend
npm run dev
```
Фронтенд будет доступен на `http://localhost:5173` с проксированием API запросов на FastAPI.

## Шаг 5: Запуск сервера

Запустите FastAPI сервер:

```bash
# Из корня проекта
make dev-up

# Или напрямую
python app/main_app.py
```

Сервер запустится на `http://127.0.0.1:8000`

## Шаг 6: Тестирование

### Тестирование через браузер

1. Откройте `http://127.0.0.1:8000/` в браузере
2. Вы увидите страницу логина
3. Введите учетные данные:
   - **Username**: `admin`
   - **Password**: ваш ADMIN_PASSWORD из .env
4. После входа откроется Dashboard с информацией о системе

### Тестирование API через curl

#### 1. Получение JWT токена

```bash
curl -X POST http://127.0.0.1:8000/api/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=My\$ecureP@ssw0rd2025"
```

Ответ:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

Сохраните токен в переменную:
```bash
TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

#### 2. Получение информации о текущем пользователе

```bash
curl http://127.0.0.1:8000/api/users/me \
  -H "Authorization: Bearer $TOKEN"
```

Ответ:
```json
{
  "id": 1,
  "username": "admin",
  "email": "admin@qapsula.local",
  "full_name": "Administrator",
  "is_active": true,
  "is_superuser": true,
  "created_at": "2025-12-30T10:00:00",
  "last_login_at": "2025-12-30T15:30:00"
}
```

#### 3. Тестирование защищенных RAG endpoints

```bash
# Список клиентов (требует авторизации)
curl http://127.0.0.1:8000/tenants \
  -H "Authorization: Bearer $TOKEN"

# Глобальная статистика
curl http://127.0.0.1:8000/stats \
  -H "Authorization: Bearer $TOKEN"

# Инициализация нового клиента (требует admin прав)
curl -X POST http://127.0.0.1:8000/tenants/test_client/initialize \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "llm_type": "openrouter",
    "model": "openai/gpt-4o-mini"
  }'
```

#### 4. Тестирование без токена (должно вернуть 401)

```bash
curl http://127.0.0.1:8000/tenants
# {"detail":"Not authenticated"}
```

### Тестирование через Swagger UI

FastAPI автоматически генерирует интерактивную документацию:

1. Откройте `http://127.0.0.1:8000/docs`
2. Нажмите кнопку "Authorize" в правом верхнем углу
3. Введите токен в поле (формат: `Bearer <ваш_токен>`)
4. Тестируйте endpoints прямо из браузера

## Безопасность

### Защищенные endpoints

Все RAG API endpoints требуют авторизации:
- `/` - информация об API (требует токен)
- `/chat` - чат с документами (требует токен)
- `/documents/upload` - загрузка документов (требует токен)
- `/documents/search` - поиск по документам (требует токен)
- `/tenants` - список клиентов (требует токен)
- `/tenants/{id}/stats` - статистика клиента (требует токен)
- `/stats` - глобальная статистика (требует токен)

**Admin-only endpoints** (требуют is_superuser=True):
- `/tenants/{id}/initialize` - инициализация клиента
- `/tenants/{id}/reload` - перезагрузка клиента

**Незащищенные endpoints:**
- `/health` - проверка здоровья (для мониторинга)
- `/api/login` - авторизация
- `/api/logout` - выход

### Хранение токенов

**В браузере:**
- Токены хранятся в `localStorage`
- При выходе токен удаляется
- При истечении срока (30 минут по умолчанию) пользователь автоматически перенаправляется на login

**Безопасность:**
- JWT токены подписаны SECRET_KEY
- Пароли хешируются с помощью bcrypt (12 rounds)
- Используется OAuth2 Bearer flow
- Токены stateless - не хранятся на сервере

### Рекомендации для production

1. **HTTPS обязателен** - используйте SSL сертификат
2. **SECRET_KEY** - никогда не коммитьте в git
3. **CORS** - ограничьте `allow_origins` конкретными доменами
4. **Rate limiting** - добавьте ограничение на `/api/login`
5. **Refresh токены** - для длительных сессий
6. **Логирование** - логируйте попытки входа
7. **httpOnly cookies** - более безопасная альтернатива localStorage

## Устранение проблем

### Ошибка: "SECRET_KEY не установлен"

Убедитесь что `.env` файл существует и содержит `SECRET_KEY`:
```bash
cat .env | grep SECRET_KEY
```

### Ошибка: "ADMIN_PASSWORD не установлен"

Добавьте `ADMIN_PASSWORD` в `.env` и пересоздайте admin:
```bash
echo "ADMIN_PASSWORD=MySecurePassword" >> .env
python scripts/create_admin.py
```

### Ошибка: "Admin пользователь уже существует"

Это нормально - admin уже создан. Используйте существующий пароль или удалите пользователя из БД:
```sql
DELETE FROM users WHERE username = 'admin';
```

### Фронтенд не загружается

1. Проверьте что фронтенд собран:
```bash
ls -la app/static/
# Должны быть index.html и assets/
```

2. Пересоберите фронтенд:
```bash
cd app/frontend
npm install
npm run build
```

### 401 Unauthorized при запросах

1. Проверьте что токен валиден и не истек
2. Проверьте формат заголовка: `Authorization: Bearer <token>`
3. Проверьте что пользователь активен (`is_active=true`)

## Дальнейшие улучшения

- [ ] Реализовать refresh токены для длительных сессий
- [ ] Добавить регистрацию пользователей (если нужно)
- [ ] Реализовать смену пароля
- [ ] Добавить email подтверждение
- [ ] Реализовать "Забыли пароль?"
- [ ] Добавить 2FA (двухфакторную аутентификацию)
- [ ] Мультитенантность на уровне пользователей
- [ ] Role-based access control (RBAC) с гранулярными правами

## Структура БД

### Таблица users

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(100) UNIQUE NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    hashed_password VARCHAR(255) NOT NULL,
    full_name VARCHAR(255),
    is_active BOOLEAN DEFAULT TRUE,
    is_superuser BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    last_login_at TIMESTAMP
);
```

## API Endpoints

### Authentication

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| POST | `/api/login` | Получить JWT токен | - |
| GET | `/api/users/me` | Информация о текущем пользователе | ✓ |
| POST | `/api/logout` | Выход из системы | ✓ |

### RAG API

| Метод | Endpoint | Описание | Auth | Admin |
|-------|----------|----------|------|-------|
| GET | `/` | Информация об API | ✓ | - |
| GET | `/health` | Проверка здоровья | - | - |
| POST | `/chat` | Чат с документами | ✓ | - |
| POST | `/documents/upload` | Загрузка документа | ✓ | - |
| GET | `/documents/search` | Поиск по документам | ✓ | - |
| GET | `/tenants` | Список клиентов | ✓ | - |
| GET | `/tenants/{id}/stats` | Статистика клиента | ✓ | - |
| POST | `/tenants/{id}/initialize` | Инициализация клиента | ✓ | ✓ |
| POST | `/tenants/{id}/reload` | Перезагрузка клиента | ✓ | ✓ |
| GET | `/stats` | Глобальная статистика | ✓ | - |

---

**Готово!** Система авторизации настроена и готова к использованию.