# 🚀 Быстрый старт

5 минут до запущенного бота!

## 📋 Что вам понадобится

**Вариант 1: Локальная модель (РЕКОМЕНДУЕТСЯ для старта)** ⭐
- Telegram Bot Token от @BotFather
- 6+ GB свободной RAM
- 5 GB свободного места на диске

**Вариант 2: OpenAI API (платно)**
- Telegram Bot Token от @BotFather
- OpenAI API Key + баланс $5+

## 🖥 Локальный запуск (для теста)

### Шаг 1: Клонирование

```bash
git clone https://github.com/your-username/telegram-rag-bot.git
cd telegram-rag-bot
```

### Шаг 2: Установка зависимостей

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### Шаг 3: Настройка

**Telegram Token (обязательно):**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Скопируйте токен

**Настройка .env:**
```bash
cp .env.example .env
nano .env  # или используйте любой редактор
```

**Для локальной модели (без OpenAI):**
```env
TELEGRAM_BOT_TOKEN=ваш_телеграм_токен
USE_LOCAL_MODEL=true
LOCAL_MODEL_PATH=./models/saiga_llama3_8b.Q4_K_M.gguf
MODEL_TYPE=saiga
```

### Шаг 4: Скачивание модели

**Linux/Mac:**
```bash
chmod +x download_model.sh
./download_model.sh
# Выберите вариант 1 (Saiga - рекомендуется)
```

**Windows:**
```bash
download_model.bat
# Выберите вариант 1
```

**Или вручную:**
Скачайте с [HuggingFace](https://huggingface.co/IlyaGusev/saiga_llama3_8b_gguf/resolve/main/saiga_llama3_8b.Q4_K_M.gguf) и поместите в папку `models/`

### Шаг 4: Запуск

```bash
python -m app.main
```

Готово! 🎉 Найдите вашего бота в Telegram и напишите `/start`

**Первый запуск займет ~1-2 минуты для загрузки модели в память.**

## 🐳 Docker запуск (рекомендуется)

### Шаг 1-3: Как выше

### Шаг 4: Запуск в Docker

```bash
docker-compose up -d
```

Проверка:
```bash
docker-compose logs -f
```

## 🌐 Деплой на сервер

### Быстрый способ

1. Подключитесь к серверу:
```bash
ssh root@your-server-ip
```

2. Скопируйте и выполните:
```bash
cd /opt
git clone https://github.com/your-username/telegram-rag-bot.git
cd telegram-rag-bot
cp .env.example .env
nano .env  # Вставьте токены
chmod +x deploy.sh
./deploy.sh
```

3. Готово! Бот работает 24/7

## ✅ Проверка работы

Откройте бота в Telegram и попробуйте:

```
/start
Привет!
Что такое машинное обучение?
Расскажи про Python
```

## 📚 Добавление знаний (опционально)

1. Создайте текстовый файл:
```bash
mkdir -p data/documents
nano data/documents/knowledge.txt
```

2. Вставьте текст и сохраните

3. Перезапустите бота:
```bash
# Локально
python -m app.main

# Docker
docker-compose restart
```

Теперь бот будет отвечать на основе ваших документов!

## 🆘 Проблемы?

### Бот не отвечает
- Проверьте токен в .env
- Проверьте логи: `docker-compose logs -f`

### Ошибка OpenAI
- Проверьте API ключ
- Убедитесь, что баланс > $0
- Проверьте лимиты: [platform.openai.com/usage](https://platform.openai.com/usage)

### Другие проблемы
- Смотрите [README.md](README.md)
- Смотрите [DEPLOY.md](DEPLOY.md)
- Создайте [issue на GitHub](https://github.com/your-username/telegram-rag-bot/issues)

## 🎓 Что дальше?

- Добавьте больше документов
- Настройте параметры в `app/main.py`
- Изучите архитектуру в `README.md`
- Прочитайте про RAG в документации

## 💡 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление кода
git pull && docker-compose up -d --build
```

## 🎉 Готово!

Теперь у вас есть AI-бот с RAG! Удачи с акселератором! 🚀
