# 🚀 Быстрый старт
## 📖 Документация

- **README.md** - обзор проекта, архитектура, возможности
- **QUICKSTART.md** - этот файл (быстрый старт за 5 минут)
- **docs/AUTH_SETUP.md** - подробное руководство по JWT авторизации
- **docs/deploy_guide.md** - развертывание на production сервер
- **docs/git_commands.md** - полезные Git команды

## 📋 Что вам понадобится

**Вариант 1: OpenRouter (РЕКОМЕНДУЕТСЯ)** ⭐
- Telegram Bot Token от @BotFather
- OpenRouter API Key (дешево, много моделей)
- От $0.01 на балансе

**Вариант 2: OpenAI API**
- Telegram Bot Token от @BotFather
- OpenAI API Key + баланс $5+

## 🐳 Запуск через Docker (РЕКОМЕНДУЕТСЯ)

Самый простой способ - всё уже настроено, включая PostgreSQL!

### Шаг 1: Клонирование

```bash
git clone https://github.com/your-username/qapsula.git
cd qapsula
```

### Шаг 2: Получение API ключей

**Telegram Bot Token:**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Скопируйте токен

**OpenRouter API Key (рекомендуется):**
1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Пополните баланс на $1-5
3. Создайте API ключ в настройках

**Или OpenAI API Key:**
1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com/)
2. Создайте API ключ

### Шаг 3: Настройка .env

```bash
cp .env.example .env
nano .env  # или vim .env
```

**Минимальная конфигурация:**
```env
# Telegram бот
TELEGRAM_BOT_TOKEN=ваш_телеграм_токен

# LLM (OpenRouter рекомендуется)
LLM_TYPE=openrouter
OPENROUTER_API_KEY=ваш_openrouter_ключ
OPENROUTER_MODEL=openai/gpt-4o-mini

# Или OpenAI
# LLM_TYPE=openai
# OPENAI_API_KEY=ваш_openai_ключ

# PostgreSQL (для Docker можно оставить по умолчанию)
POSTGRES_PASSWORD=postgres_dev_password
```

### Шаг 4: Запуск

```bash
# Запуск в development режиме
make dev-up

# Или напрямую
docker-compose -f docker-compose.dev.yml up -d
```

### Шаг 5: Применение миграций БД (первый запуск)

```bash
# Применить миграции для создания таблиц
make db-upgrade

# Или напрямую
docker-compose -f docker-compose.dev.yml exec app alembic upgrade head
```

### Шаг 6: Настройка авторизации 🔐

Система защищена JWT авторизацией. Нужно создать admin пользователя:

**1. Добавьте в .env:**
```bash
nano .env
```

Добавьте:
```env
# === JWT Авторизация ===
SECRET_KEY=<сгенерируйте командой: openssl rand -hex 32>
ADMIN_PASSWORD=ваш_надежный_пароль_для_admin
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

**2. Создайте admin пользователя:**
```bash
# Из Docker
docker-compose -f docker-compose.dev.yml exec app python scripts/create_admin.py

# Локально
python scripts/create_admin.py
```

**3. Соберите Vue.js фронтенд:**
```bash
cd app/frontend
npm install
npm run build
cd ../..
```

Готово! 🎉

**Проверка:**
- **Веб-интерфейс**: Откройте http://127.0.0.1:8000 (войдите как admin)
- **API документация**: http://127.0.0.1:8000/docs (нажмите "Authorize", введите токен)
- **Бот**: Найдите бота в Telegram и напишите `/start`
- **БД**: `psql -h localhost -p 5432 -U postgres -d qapsula_dev` (пароль: postgres_dev_password)

**Подробная документация по авторизации:** см. `docs/AUTH_SETUP.md`

**Логи:**
```bash
make logs
# Или
docker-compose -f docker-compose.dev.yml logs -f
```

**Остановка:**
```bash
make dev-down
```

## 💻 Локальный запуск (БЕЗ Docker)

Для разработки без Docker нужна локальная PostgreSQL.

### Требования:
- Python 3.11+
- PostgreSQL 14+ (установленная локально)

### Шаги:

**1. Клонирование и зависимости:**
```bash
git clone https://github.com/your-username/qapsula.git
cd qapsula

# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

**2. Настройка PostgreSQL:**

Создай БД локально:
```bash
# Подключись к PostgreSQL
psql -U postgres

# Создай базу данных
CREATE DATABASE qapsula;
\q
```

**3. Настройка .env:**
```bash
cp .env.example .env
nano .env
```

Обязательно укажи DATABASE_URL для локальной БД:
```env
# Telegram и LLM как в Docker варианте выше

# PostgreSQL - для локального подключения
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/qapsula
```

**4. Применение миграций:**
```bash
alembic upgrade head
```

**5. Запуск:**
```bash
python -m app.main_app
```

Готово! API на http://127.0.0.1:8000

## 📊 Production запуск

Для production используй `docker-compose.yml` (без dev):

```bash
# 1. Настрой .env с безопасными паролями
nano .env

# POSTGRES_PASSWORD=strong_password_here
# TELEGRAM_BOT_TOKEN=your_token

# 2. Запусти
make prod-up

# Или
docker-compose up -d

# 3. Примени миграции
make db-upgrade
```

Production отличия:
- Параметры БД из .env (безопасность)
- Папка `backups/` для бэкапов БД
- Без hot reload кода

## 🌐 Деплой на сервер

См. подробную инструкцию в `docs/deploy_guide.md`

**Кратко:**
1. Установи Docker на сервер
2. Склонируй репозиторий
3. Настрой `.env` с продакшн паролями
4. Запусти `make prod-up`
5. Примени миграции `make db-upgrade`

## ✅ Проверка работы

Откройте бота в Telegram и попробуйте:

```
/start
Привет!
Что такое машинное обучение?
Расскажи про Python
```

## 📚 Добавление знаний (опционально)

**Для Docker:**
```bash
# 1. Создай документ
mkdir -p data/token/documents
nano data/token/documents/knowledge.txt
# Вставь текст и сохрани

# 2. Перезапусти для переиндексации
make dev-down && make dev-up
```

**Для локального:**
```bash
# 1. Создай документ
mkdir -p data/token/documents
nano data/token/documents/knowledge.txt

# 2. Перезапусти
python -m app.main_app
```

Где `token` - это tenant_id (название клиента).
Бот автоматически проиндексирует документы при запуске!

## 🆘 Проблемы?

### БД не доступна
```bash
# Проверь что PostgreSQL запущена
docker ps | grep postgres

# Должно быть: qapsula-postgres-dev (healthy)
```

### Миграции не применились
```bash
# Примени миграции вручную
make db-upgrade

# Или через Docker
docker-compose -f docker-compose.dev.yml exec app alembic upgrade head
```

### Бот не отвечает
- Проверь токен в .env
- Проверь логи: `make logs`
- Убедись что применены миграции БД

### Ошибка OpenRouter/OpenAI
- Проверь API ключ в .env
- Убедись что баланс > $0
- OpenRouter: [openrouter.ai/credits](https://openrouter.ai/credits)
- OpenAI: [platform.openai.com/usage](https://platform.openai.com/usage)

### Порт 5432 занят
Если у тебя уже запущен PostgreSQL локально:
```bash
# Измени порт в docker-compose.dev.yml
ports:
  - "5433:5432"  # Вместо 5432:5432
```

### Другие проблемы
- Смотри README.md - общая документация проекта

## 🎓 Что дальше?

- Добавьте больше документов для RAG
- Настройте мультитенантность (разные клиенты)
- Попробуйте разные модели через OpenRouter
- Изучите FastAPI эндпоинты
- Изучите архитектуру в `README.md`

## 💡 Полезные команды

**Development:**
```bash
# Запуск
make dev-up

# Логи
make logs

# Остановка
make dev-down

# Миграции (автоматически через Docker)
make db-migrate msg="описание"  # Создать новую миграцию
make db-upgrade                  # Применить все миграции
make db-downgrade               # Откатить последнюю миграцию

# Перезапуск после изменений
make dev-down && make dev-up

# Обновление кода
git pull && make dev-down && make dev-up
```

**Production:**
```bash
# Запуск
make prod-up

# Остановка
make prod-down

# Обновление
git pull && make prod-down && docker-compose up -d --build
```

**Все команды:**
```bash
make help  # Показать все доступные команды
```

## 🎉 Готово!

Теперь у вас есть AI-бот с RAG! Удачи с акселератором! 🚀
