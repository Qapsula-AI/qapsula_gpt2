services:
  telegram-bot:
    build: .
    container_name: telegram-rag-bot
    restart: unless-stopped
    environment:
      - PYTHONPATH=/app
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: python -m app.main_app