version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-rag-bot
    restart: unless-stopped
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./models:/app/models  
    ports:
      - "8000:8000"
    command: >
      sh -c "
      echo '🚀 Запуск FastAPI на порту 8000...' &&
      uvicorn app.api.fastapi_app:app --host 0.0.0.0 --port 8000 --reload &
      UVICORN_PID=$$! &&
      echo '🤖 Запуск Telegram бота...' &&
      python -u -m app.main_app ||
      (echo '❌ Telegram бот упал, останавливаем FastAPI...' && kill $$UVICORN_PID)
      "